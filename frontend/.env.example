# Base URL of the CampusFlow backend API.
# Leave empty in local development to use the CRA dev proxy (package.json "proxy").
# In production set it to your deployed backend, e.g. https://api.campusflow.com
REACT_APP_API_URL=
